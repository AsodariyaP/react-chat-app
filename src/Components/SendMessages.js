
import { useMutation } from '@apollo/client';
import { POST_MESSAGES } from '../GraphQL/Mutations';

const SendMessages = () => {
    return (
        <div></div>
    );
};

export default SendMessages;